#include "main.h"
/**
 *_isalpha-checks for alphabetic character
 *@c:character to check
 *Return:integer
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (0);
	}
	else
	{
		return (0);
	}

}
